#include <iostream>
#include <math.h>
using namespace std;

/**
 * This code prompt the user to enter a radius
 * The user will input the raidus
 * The volume and surface_area variables are defined
 * The formulas for the volume and surace_areas are written out to be evaluated
 * The code outputs volume and surace_area respectively depending on the input. 
*/ 

int main()
{
    // cout << "Enter a radius: " << endl;
    // float radius;
    // cin >> radius;
    // float volume;
    // float surface_area;
    // volume = (4.0/3.0) * M_PI * pow(radius, 3);
    // surface_area = 4 * M_PI * pow(radius,2);
    // cout << "volume: " << volume << endl;
    // cout << "surface_area: " << surface area << endl;
    
string s = "1234";
for (int i = 0; i <= 4; i++)
{
   cout << s.substr (i, 1);
}
}